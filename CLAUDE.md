# CLAUDE.md

このリポジトリで作業する際のルール。

## ツール呼び出しの手順（重要）

過去に、ツール呼び出しの開始タグを壊れた形式で出力して実行が弾かれ（malformed）、作業が止まる不具合が頻発した。再発を構造的に防ぐため、以下を守る。

- **ツール呼び出しは応答の先頭で出す。前置きの説明文はツール実行後（結果が返ってから）に書く。**
- **独立した操作は1つの応答でまとめて出す。**
- **ツール呼び出しが malformed で弾かれたら、即座に正しい形式で再送し、停止したまま放置しない（ユーザーを待たせない）。**

## 機能実装コミット時の確認（重要）

機種の**新機能・情報更新**を実装してコミットする際は、**毎回こちらから次の2点をユーザーに確認する**（ユーザーからの指示を待たない。過去に Tip の追加漏れが発生したため）。

### 1. TipKit の Tip を追加するか
追加する場合：
- **定義**：開発中バージョンの `SetteiSaCountApp/Common/class/verNNNN.swift` に `struct tipVerNNNN<Prefix><機能名>: Tip` を追加。
  - `title`：カウント機能等の追加＝`"機能更新"` ／ 解析情報の更新＝`"情報更新"`
  - `message`：何が変わったかを1〜2行（例 `"小役確率・ボーナス重複当選率のカウント機能を追加しました"`）
  - `image`：`Image(systemName: "exclamationmark.bubble")`（新機種/機能追加の告知は `"star"`）
- **配置**：その機能の中心となる UI 要素に `.popoverTip(tipVerNNNN...())` を付ける（セグメントピッカー／確率結果ユニット／カウントボタン等）。
- **旧Tipの無効化**：**同一ページ内に旧バージョンの `.popoverTip` があればコメントアウトして無効化**する。`commonTip*`（バージョン非依存の常設Tip）は対象外。
- **通常は不要なケース**：新規ページ追加のみ（add-page）、新機種追加のみ。

### 2. 通知バッジ（`verNNNNFirstLaunch()`）に追加するか
既存機種の更新を既存ユーザーに知らせる場合、`commonVar.swift` の開発中バージョンの関数へ：
- `machines.updateMachineBadgeStatus(id: "<機種ID>", newStatus: "update")`
- 更新したページの `<prefix>Menu<PageName>Badge = "update"`（新規ページは `"new"`）

**設定期待値ページのバッジ（`<prefix>MenuBayesBadge`）は基本的に対象外**。変数自体は従来どおり add-machine で生成するが、ほぼ運用していないため `verNNNNFirstLaunch()` への追加は不要（ベイズに判別要素を足しただけの更新では、バッジ確認の提案もしなくてよい）。ユーザーから明示指示があった場合のみ追加する。
